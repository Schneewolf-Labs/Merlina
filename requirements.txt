# Core web framework
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
pydantic>=2.0.0
pydantic-settings>=2.0.0
python-multipart>=0.0.6

# ML/Training frameworks
# NOTE: torch is NOT listed here. GPU environments (RunPod, Colab, etc.) ship
# with a torch+CUDA build paired to torchvision/torchaudio. Letting pip upgrade
# torch independently breaks those companion packages. Install torch separately
# if needed: pip install torch>=2.0.0
transformers>=5.0.0
accelerate>=0.24.0
grimoire-rl @ git+https://github.com/Schneewolf-Labs/grimoire.git
peft>=0.6.0
bitsandbytes>=0.41.0
datasets>=2.14.0

# Utilities
wandb>=0.15.0
huggingface-hub>=0.19.0
sentencepiece>=0.1.99
protobuf>=3.20.0
scipy>=1.10.0
einops>=0.7.0

# Optional but recommended
# xformers is excluded for the same reason as torch - it is tightly coupled to
# the torch+CUDA version. Install it manually if needed: pip install xformers
ninja>=1.11.0     # For faster builds
packaging>=23.0   # Version handling
pynvml>=11.5.0    # For detailed GPU monitoring