# Core web framework
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
pydantic>=2.0.0
pydantic-settings>=2.0.0
python-multipart>=0.0.6

# ML/Training frameworks
# torch, torchvision, and torchaudio must be installed together so they stay in
# sync. On GPU environments, install with the correct CUDA index first:
#   pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu124
# Then run: pip install -r requirements.txt
torch>=2.5.0
torchvision
torchaudio
transformers>=5.0.0
accelerate>=0.24.0
grimoire-rl @ git+https://github.com/Schneewolf-Labs/grimoire.git
peft>=0.6.0
bitsandbytes>=0.41.0
datasets>=2.14.0

# Utilities
wandb>=0.15.0
huggingface-hub>=0.19.0
sentencepiece>=0.1.99
protobuf>=3.20.0
scipy>=1.10.0
einops>=0.7.0

# Optional but recommended
# xformers is tightly coupled to torch+CUDA. Install manually after torch:
#   pip install xformers
ninja>=1.11.0     # For faster builds
packaging>=23.0   # Version handling
pynvml>=11.5.0    # For detailed GPU monitoring